import os, sys, jsonnn# 입력 파일에서 인자 파싱ntry:n    with open(sys.argv[1], 'r') as f:n        args = json.load(f)nexcept Exception as e:n    print(f'Error parsing input: {e}')n    sys.exit(1)nn# 디렉토리 목록 및 크기 계산nfiles_info = []nfor root, dirs, files in os.walk('.'):n    for name in files:n        filepath = os.path.join(root, name)n        try:n            size = os.path.getsize(filepath)n            files_info.append({